version: '3'

services:
  nginx:
    build: requirements/nginx/.
    container_name: nginx
#    depends_on:
#      - wordpress
    ports:
      - "443:443"
    # volumes:
    #   - ./requirements/nginx/conf/:/etc/nginx/http.d/
    #   - ./requirements/nginx/tools:/etc/nginx/ssl/
    #   - /home/yaidriss42/Desktop/simple_docker_nginx_html/public/html:/var/www/
    restart: always
  mariadb:
    build: requirements/mariadb/.
    container_name: mariadb
    env_file:
      - .env
    
      # args:
      #   DB_NAME: ${DB_NAME}
      #   DB_USER: ${DB_USER}
      #   DB_PASS: ${DB_PASS}
      #   DB_ROOT: ${DB_ROOT}
    ports:
      - "3306:3306"
  #   volumes:
  #     -db-volume: /var/lib/mysql
  #   restart: always